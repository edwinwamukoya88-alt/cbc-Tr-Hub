import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: {
    default: "CBC Teachers Hub - AI-Powered CBC Teaching Tools",
    template: "%s | CBC Teachers Hub",
  },
  description: "AI-powered tools for Kenyan CBC teachers. Generate lesson plans, exams, schemes of work, report cards, and more — all aligned to KICD standards.",
  keywords: ["CBC", "Kenya", "teacher", "lesson plan", "KICD", "education", "AI"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`} suppressHydrationWarning>
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
